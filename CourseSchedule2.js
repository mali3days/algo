/**
 * @param {number} numCourses
 * @param {number[][]} prerequisites
 * @return {number[]}
 */

function CourseSchedule2(numCourses, prerequisites) {
  // TODO: implement the logic
  // return [] if it is impossible to finish all courses
}

CourseSchedule2(4, [
  [1, 0],
  [2, 0],
  [3, 1],
  [3, 2],
]); // => [0,2,1,3]
